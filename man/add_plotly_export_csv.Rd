% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_plotly_export_csv.R
\name{add_plotly_export_csv}
\alias{add_plotly_export_csv}
\title{Decorate a plotly htmlwidget with a Modebar "Download CSV" button}
\usage{
add_plotly_export_csv(
  widget,
  filename = "plotly-data.csv",
  visible_only = TRUE,
  include_text = TRUE,
  include_z = FALSE,
  include_customdata = FALSE,
  button_title = "Download data as CSV",
  button_name = "exportCsv"
)
}
\arguments{
\item{widget}{A plotly htmlwidget (e.g. from \code{\link[plotly:ggplotly]{plotly::ggplotly()}} or \code{\link[plotly:plot_ly]{plotly::plot_ly()}}).}

\item{filename}{Filename suggested to the browser for the downloaded CSV.}

\item{visible_only}{If \code{TRUE}, export only traces currently visible (respects legend toggles).}

\item{include_text}{If \code{TRUE}, include a \code{text} column when trace text is present.}

\item{include_z}{If \code{TRUE}, include a \code{z} column when trace z is present.}

\item{include_customdata}{If \code{TRUE}, include a \code{customdata} column when present.}

\item{button_title}{Tooltip text shown on hover.}

\item{button_name}{Internal button name (used to avoid duplicate buttons on re-render).}
}
\value{
The modified widget.
}
\description{
Adds a custom Plotly modebar button that downloads the currently-rendered
trace data as a CSV file. This works in static HTML contexts such as Quarto
and R Markdown (no Shiny server required).
}
\details{
The exported CSV is based on \code{gd.data} (Plotly traces) available in the
browser at render time, i.e. what the plotly widget is drawing.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE) &&
    requireNamespace("plotly", quietly = TRUE) &&
    requireNamespace("magrittr", quietly = TRUE)) {
  library(ggplot2)
  library(plotly)
  library(magrittr)

  p <- ggplot(mtcars, aes(wt, mpg, colour = factor(cyl))) + geom_point()
  ggplotly(p) \%>\%
    add_plotly_export_csv(filename = "mtcars.csv")
}
}
